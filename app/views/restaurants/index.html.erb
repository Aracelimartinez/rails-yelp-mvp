
<div class="container">

  <h1>Restaurants</h1>

  <ul class="list-group">
    <% @restaurants.each do |restaurant| %>
      <li class="list-group-item"><%= link_to restaurant.name.capitalize, restaurant_path(restaurant) %></li>
    <% end %>
  </ul>

  <%= link_to "New restaurant", new_restaurant_path %>

</div>
